// https://dmoj.ca/problem/ccc18s3

#include <bits/stdc++.h>
using namespace std;
#define endl "\n"

int main() {
	cin.sync_with_stdio(0);
	cin.tie(0);
	
	int n, m; cin >> n >> m;

	vector<vector<pair<int, int>>> m(n, vector<pair<int, int>>(m));
	vector<string> b(n);

	for(int i = 0; i < n; i++) {
		cin >> b[i];
	}	

	for(int i = 0; i < n; i++) {
		for(int j = 0; j < m; j++) {
			
		}
	}
}