// https://dmoj.ca/problem/ccc96s3

#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.sync_with_stdio(0);
	cin.tie(0);
	
	int n, a, b, p[8] = {0}
	vector<int> c(30);
	cin >> n;

	for(int i = 0; i < n; i++) {
		
	}

	cout << c[2];
}